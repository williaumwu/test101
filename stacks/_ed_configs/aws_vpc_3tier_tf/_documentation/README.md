**Description**
  - The stack that creates a 3tier VPC with terraform templates

**Required**

vpc_name

**Optional**

aws_default_region: us-east-1
cidr - string
stateful_id - string
availability_zones - list converted to string
private_subnets - list converted to string
public_subnets - list converted to string
database_subnets - list converted to string
```
